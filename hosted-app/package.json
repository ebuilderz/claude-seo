{
  "name": "claude-seo-upstream-host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal private web host for the unmodified upstream Claude SEO workflow.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node app/server.js",
    "test": "node --test",
    "check": "node --check app/server.js && node --check app/jobs.js && node --check app/auth.js && node --check app/report.js && node --check app/url-safety.js"
  },
  "dependencies": {
    "express": "5.1.0",
    "helmet": "8.1.0",
    "ipaddr.js": "2.2.0",
    "jose": "6.1.2",
    "markdown-it": "14.3.0",
    "sanitize-html": "2.17.0"
  }
}
