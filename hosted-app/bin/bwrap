#!/bin/sh
set -eu

# The outer seoauditcontainer AppArmor profile explicitly permits user
# namespaces. The container entrypoint sets no_new_privs, which intentionally
# prevents later AppArmor profile changes, so launch bubblewrap directly.
exec /usr/bin/bwrap "$@"
